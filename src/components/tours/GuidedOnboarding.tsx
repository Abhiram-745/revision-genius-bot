// GuidedOnboarding is disabled to simplify the tour system
// The ProductTour component via TourManager handles page-specific tours

const GuidedOnboarding = () => {
  // Disabled - return null to prevent any tours from this component
  return null;
};

export default GuidedOnboarding;